The benchmarks folder contains code for conducting experiments, datasets and results obtained.
The lib folder consists of the C libraries that are wrapped via Cython in the .pyx and .pxd files and further compiled in order to being called from Python.
